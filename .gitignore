# dependencies
/node_modules

# build output — dist/ IS committed: the Claude Code plugin runs it directly
# (self-contained bundle via tsup `noExternal`). Rebuild with `npm run build`
# before committing changes.
*.tsbuildinfo

# env / secrets
.env*

# misc
.DS_Store
npm-debug.log*

# local MCP registry publisher binary (not committed)
mcp-publisher
